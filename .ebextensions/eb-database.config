option_settings:
  aws:elasticbeanstalk:application:environment:
    DATABASE_URL: !Ref DATABASE_URL
    PGUSER: !Ref DATABASE_USERNAME
    PGPASSWORD: !Ref DATABASE_PASSWORD
    PGDATABASE: !Ref DATABASE_NAME
    PGHOST: !Ref DATABASE_HOST
    PGPORT: !Ref DATABASE_PORT